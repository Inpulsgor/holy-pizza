import { FC } from "react";
import styled from "styled-components";

const Footer = styled.footer`
  grid-area: footer;
`;

const AppBasement: FC = () => {
  return <Footer>AppBasement</Footer>;
};

export default AppBasement;
