export enum LoadingStatus {
  IDLE = "IDLE",
  LOADING = "LOADING",
}

export enum RequestStatus {
  IDLE = "IDLE",
  PENDING = "PENDING",
  SUCCEEDED = "SUCCEEDED",
  FAILED = "FAILED",
}

export enum LangType {
  EN = "EN",
  UA = "UA",
}
